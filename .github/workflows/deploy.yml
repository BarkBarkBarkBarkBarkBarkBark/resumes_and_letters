name: Build and Deploy Resume Site

on:
  push:
    branches: [main, master]
  workflow_dispatch:      # allows manual trigger from the Actions tab

# Required for the deploy-pages action
permissions:
  contents: read
  pages: write
  id-token: write

# Only one deploy at a time; cancel in-flight runs on new push
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  # ── Build ────────────────────────────────────────────────────────────────────
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: resume    # all run steps execute inside resume/

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: resume/requirements.txt

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Build cover letter
        run: python scripts/build_cover_letter.py --company "UC Davis" --role "Research Engineer" --profile neuroengineering --md-only

      - name: Build site
        run: python scripts/build_site.py --profile neuroengineering

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: resume/docs/         # relative to repo root

  # ── Deploy ───────────────────────────────────────────────────────────────────
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
